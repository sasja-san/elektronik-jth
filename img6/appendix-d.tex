
\begin{circuitikz}[scale=1.15, voltage shift=2.5]
  \ctikzset{
    logic ports=european,
    tripoles/european not symbol=ieee circle,
    logic ports/scale=1.0,
    multipoles/flipflop/clock wedge size=0.25,
  }
  \draw % X=5, Y=3
    (0,0) node[anchor=east]{} coordinate (btmL)
      to[sV, l=\ix{V}{sig}] ++ (0,+1.5)
      to[R=50<\ohm>] ++ (0,+1.5)
      to[short]  ++ (+5,0)
      to[short, -*] ++ (0,-0.5) coordinate (rSplit)
      % vänster sida:
      to[short] ++ (-1.5,0)
      to[R=\ix{R}{term}, a=50<\ohm>] ++ (0,-2)
      to[short, -*] ++ (+1.5,0) coordinate (rJoin)
      % höger sida
    (rSplit)
      to[short] ++ (+1.5,0)
      to[R=\ix{R}{osc}, a=1<\mega\ohm>] ++ (0,-2)
      to[short] ++ (-1.5,0)
      % 
      to[short] ++ (0,-0.5)
      to[short]  ++ (-5,0) 
    ;
\end{circuitikz}

